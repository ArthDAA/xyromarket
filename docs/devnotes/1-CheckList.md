# 1-CheckList.md - Xyro Market

| PACT Phase I -> Procedure for Architecture Contracts and Typing
| Author: Arthus De Assis-Allix
| Subject: Mission AllôStaff — plateforme d'échange/don de serveurs Discord (client: Le_Club)
| Date: 2026-09-04 -> Ready for Phase II : `2-Architecture.md`
| Statut : LOCKED — prêt pour Phase II

---

## 1. Formats

| # | Element | Value | Source |
|---|---|---|---|
| F1 | Hébergement | By-Hoster, VPS KVM Proxmox — projet infra from scratch, pas de reprise de l'existant Cloudflare Workers | Contrainte perso Arthus + confirmation client |
| F2 | Stack bot | Node.js + discord.js, Gateway persistante (pas interactions HTTP-only) | Décision dev, cohérente avec F1 |
| F3 | Base de données | PostgreSQL local sur le VPS | Décision dev |
| F4 | Paiement | Zéro système de paiement — don/troc uniquement | Client (msg 19) + footer légal ("actuellement gratuit") |
| F5 | Vérification Discord | Dossier de vérification app à préparer avant 100 guildes | Contrainte plateforme Discord |
| F6 | Nom du projet | Xyro Market | Client — corrige "Xparia" (obsolète) |
| F7 | Domaine | Nouveau nom de domaine à définir/acquérir, hors architecture v1 sauf précision ultérieure | Client — tâche différée |

## 2. Mandatory

| # | Item | Source |
|---|---|---|
| M1 | OAuth2 (`identify`+`guilds`) — lister les serveurs possédés par l'utilisateur | Conception dev |
| M2 | Bot ajouté à chaque guilde listée — vérification continue de `owner_id` | Conception dev |
| M3 | Listing : thème (tags libres, hashtag) + description + mode (`don`\|`échange`) + taille communauté (informatif) + critères recherchés (échange uniquement) | Client (msg 27) + A1/A2/A11 |
| M4 | Moteur de matching : file d'attente simple (don), graphe + cycles n-aires TTC (échange) — les deux dès v1 | Client (msg 27) + A6/A8 |
| M5 | Chat : threads privés dans un serveur hub Discord, contrôlé par le bot | Client (msg 24, 27) + A4 |
| M6 | Témoin de transfert : bot observe et horodate le bascule `owner_id` des deux côtés | Conception dev |
| M7 | Annonce automatique de passation postée par le bot dans le serveur repris | A5 |
| M8 | Pages légales/RGPD : mentions légales, CGU, confidentialité, cookies + gestion, propriété intellectuelle, données personnelles, exercice droits RGPD, suppression données, politique sécurité, règlement Xyro Market, règles Discord, anti-fraude, anti-abus, signalement, droit de rétractation (clause "gratuit pour l'instant") | Client (footer fourni) |
| M9 | Système de réputation : notes après transaction, avis/commentaires, total avis, moyenne, historique par utilisateur, tag "Vérifié" à conditions paramétrables par l'admin (retirable/modifiable), protection anti-faux-avis et anti-auto-évaluation | Client (doc détaillé) — réouverture de A3 |
| M10 | Panel administrateur complet — voir §2bis pour le détail | Client (doc détaillé) |
| M11 | Période d'essai avant transfert définitif : le bot crée/assigne un rôle "Administrateur (essai)" au destinataire pendant une durée configurable (défaut proposé : 7 jours, `[ASSUMED]` — voir A15), sans transfert réel de `owner_id` | Client (msg trial) + A14 |
| M12 | À l'issue de la période, si les deux parties valident : transfert réel effectué manuellement par le propriétaire (contrainte API inchangée, jamais automatisable) — le bot bascule alors en mode témoin (cf. M6) | Client (msg trial) + A14 |
| M13 | Annulation pendant la période d'essai : le bot révoque le rôle "Administrateur (essai)" — le propriétaire réel garde la priorité de hiérarchie sur ce rôle et peut le révoquer unilatéralement à tout moment, sans dépendre du consentement de l'autre partie | Client (msg trial) + A14 |
| M14 | Mécanisme d'enregistrement/litige pour le cas plus rare d'une annulation demandée *après* le transfert réel (`owner_id` déjà basculé) : enregistrer la demande, notifier les deux parties, vérification par l'équipe, retour effectif toujours manuel, historique complet | Client (doc détaillé) — reprend l'intention initiale du "droit de rétractation" |
| M15 | Vérification au moment de l'ajout du bot sur une guilde listée : son rôle doit être positionné au-dessus du rôle "Administrateur (essai)" qu'il devra attribuer — alerte si absent, sinon M11 échoue silencieusement | Contrainte technique Discord (hiérarchie des rôles) |

~~Snapshot cron post-transfert~~ reste retiré — sa seule fonction était de mesurer l'issue pour un scoring de réputation basé sur la croissance, ce que le système d'avis (M9) ne nécessite pas.

~~M4, volet "graphe + cycles n-aires TTC (échange)"~~ — **retiré, voir A19**. Décision utilisateur revenant explicitement sur une exigence confirmée par le client — **validée par Le_Club le 2026-09-17** (voir note A19). Le volet "file d'attente simple (don)" de M4 est inchangé.

### 2bis. Mandatory — Panel Admin (détail)

| Domaine | Fonctions |
|---|---|
| Utilisateurs | Liste, recherche, profil, annonces, historique, ban temporaire, ban définitif, débannir, suspendre, réactiver, modifier compte, gérer permissions/rôles, statut vérifié, consulter signalements — ban = plateforme uniquement (cf. A13), aucune action réelle sur Discord |
| Annonces | Liste, recherche, modifier, masquer, supprimer, restaurer, valider/refuser, marquer vérifiée, historique modifications, gérer tags (cf. A11 — gestion des hashtags, pas de taxonomie séparée) |
| Modération/Signalements | Liste/filtre signalements, ouvrir/traiter/classer un dossier, sanctionner (plateforme uniquement), notes internes, historique sanctions + traitants |
| Avis/Réputation | Liste avis, suppression avis frauduleux, signalement d'avis, statut d'avis, gestion utilisateurs vérifiés, statistiques de réputation |
| Permissions (RBAC) | Rôles Administrateur / Modérateur / Support / Gestionnaire / Propriétaire ; permissions granulaires par fonction, pas seulement par rôle fixe |
| Transactions | Liste, parties, statut, demandes de retour, dossiers de litige, notes internes, résolution, historique complet |
| Configuration site | Tags/hashtags, règles, textes affichés, rôles/permissions, paramètres modération/avis/annonces |
| Statistiques | Nb utilisateurs / annonces / transactions / signalements / bans / avis, activité récente |
| Logs/audit | Qui, quelle action, sur quoi, quand, ancienne/nouvelle valeur — trace de toute action admin importante |

## 3. Bonus

| # | Item | Targeted? | Source |
|---|---|---|---|
| B1 | Preview sans bot via `GET /invites/{code}?with_counts=true` (onboarding léger) | deferred | Conception dev |

## 4. Open Points

| # | Item | Decision | Rationale |
|---|---|---|---|
| O1 | Ratio présence/membres comme filtre de tri | Écarté | La plateforme cible des communautés endormies — l'inertie est le cas d'usage, pas l'anomalie |
| O2 | Mécanisme de chat | Threads privés en hub, pas de DM relay | Fiabilité (403 sur DM fermés/pas de guilde commune) |
| O3 | Intent `GUILD_MEMBERS` | Non demandé | Pas de scoring nécessitant le filtrage des bots dans le ratio |

## 5. Ambiguities

| # | Question | Resolution | Rationale |
|---|---|---|---|
| A1 | Taxonomie des thèmes | Tags libres, système hashtag | Réponse client |
| A2 | Taille de communauté : critère de matching ou informatif ? | Purement informatif, jamais utilisé pour matcher | Réponse client |
| A3 | Délai d'évaluation réputation | **Réouverte** — système de réputation confirmé requis (voir M9), delai d'evaluation non applicable (pas de mécanique de croissance a évaluer) | Réponse client (doc détaillé) |
| A4 | Modération du hub Discord | 100% bot | Réponse client |
| A5 | Annonce de passation | Automatique | Réponse client |
| A6 | Scope MVP (dons/échanges, n-cycles) | Don + échange dès v1, matching n-cycles dès v1, pas de version allégée | Réponse client (confirmation directe) |
| A7 | Statut juridique du projet | Aucun statut formel — responsabilité RGPD reste sur Le_Club en tant que personne physique | Réponse client — documenté dans M8 |
| A8 | Périmètre v1 | Scope complet | Réponse client |
| A9 | Nommage du projet | Xyro Market — "Xparia" (transcript msg 11) était un nom obsolète/reliquat | Réponse client |
| A10 | Infra existante (Cloudflare Workers) vs mandat By-Hoster | Projet from scratch chez By-Hoster, aucune migration/reprise de l'ancien sous-domaine | Réponse client |
| A11 | "Catégories" (doc panel) vs tags (A1) | Même mécanisme — hashtags. "Gérer les catégories" du panel = gestion des tags, pas une taxonomie séparée | Réponse client |
| A12 | "Publication directe" + "modération en amont" (contradictoires tels quels) | Publication directe, modération **en aval** (post-publication) — "valider/refuser" du panel est une action a posteriori, pas une file d'attente pré-publication | Réponse client |
| A13 | Portée du "ban" (site ou Discord réel) | Ban = plateforme uniquement, pas d'action sur le serveur Discord | Réponse client |
| A14 | Mécanisme de retour/rétractation — escrow via propriété bot demandé | Confirmé : rôle "Administrateur (essai)" pendant une période paramétrable, transfert réel manuel seulement après validation des deux parties en fin de période (cf. M11-M13) | Réponse client — confirme et étend la proposition dev |
| A15 | Durée de la période d'essai (M11) | `[ASSUMED]` : 7 jours par défaut, ensuite configurable via le panel admin (§2bis, Configuration du site) | Pas de valeur donnée par le client ("x jours") — proposition dev à valider ou ajuster |
| A16 | Ordre entre publication d'annonce et invitation du bot sur la guilde (raffinement de M2) | Publication d'abord : `listings.create()` n'exige plus `guilds.bot_present` — l'annonce est créée dans un nouveau statut `pending_bot` si le bot n'est pas encore là, et le propriétaire est redirigé directement vers l'écran d'invite Discord pré-rempli sur cette guilde (`guild_id` + `disable_guild_select`). `bot/guildWatcher.js` bascule l'annonce en `active` dès que le `guildCreate` Discord confirme le join. Une annonce `pending_bot` compte comme vivante (`uniq_listings_live_guild`) : impossible d'en publier une deuxième sur la même guilde en attendant — pas d'expiration automatique, un abandon se traduit juste par une annonce qui reste en attente, retirable par son propriétaire comme n'importe quelle annonce `active`. | Décision dev, en test live avec le client (retour : bloquer la création tant que le bot n'était pas invité était confus dans le tableau de bord — cf. `3-DebugNotes.md`, entrée du 2026-09-06) |
| A17 | Suppression d'annonce et présence du bot (complément de A16) | Symétrique de A16 : `listings.remove()` publie `intent.guild.leave` si `guilds.bot_present` — le bot quitte la guilde dès que sa seule annonce vivante est retirée, sans action manuelle du propriétaire (`guild.leave()` ne demande aucun consentement, contrairement à l'ajout). `restore()` (panel admin) rejoue la même vérification que `create()` plutôt que de remettre `active` en aveugle : si le bot est reparti entre-temps, l'annonce restaurée repart `pending_bot`. Ajout côté UI : le tableau de bord n'avait jusqu'ici aucun moyen de supprimer une annonce depuis le navigateur (`DELETE /annonces/:id` existait déjà côté API mais un `<form>` HTML ne sait pas poster en méthode `DELETE`) — nouvelle route `POST /annonces/:id/supprimer` et bouton "Supprimer" sur chaque annonce `active`/`pending_bot`/`hidden`. | Décision dev, demandée en test live ("comment supprimer une annonce ? ça doit aussi supprimer le bot") |
| A18 | Rétention des annonces `removed` | `listings.remove()` supprime désormais physiquement la ligne plutôt que de la garder indéfiniment en statut `removed` — demande explicite après avoir vu les 3 annonces `removed` traîner dans "Mes annonces" en test live. Le client a été prévenu du risque avant de trancher : `match_participants`/`listing_queue` référencent `listings(id)` en `ON DELETE RESTRICT` (une annonce ayant eu un historique de match, même annulé, ou ayant simplement reçu un candidat un jour dans sa file d'attente, même retiré depuis, bloque la suppression physique), et le suivi d'audit (M10) perd la possibilité de relire le contenu via une jointure sur `listings`. Décision retenue quand même, avec un filet de sécurité technique plutôt qu'un simple avertissement ignoré : `remove()` tente d'abord un `DELETE` réel, et ne retombe sur l'ancien comportement (`removed` en base) que si Postgres le refuse concrètement (`23503`) — jamais par choix, uniquement par contrainte physique. Le contenu intégral de l'annonce supprimée reste de toute façon lisible dans `audit_log.before` (`action: 'listing.created'` le capture déjà en entier), donc M10 n'est pas compromis même quand la ligne `listings` disparaît. | Décision utilisateur, prise en connaissance du compromis (cf. `3-DebugNotes.md`) |
| A19 | Moteur de matching automatique pour les échanges (**revient explicitement sur M4/A6, exigences confirmées par le client — validé par Le_Club le 2026-09-17, voir note en fin de ligne**) | Le moteur TTC (Top Trading Cycles, `domain/matching/ttc.js` + `preferences.js`) est **retiré** — plus aucune découverte automatique de cycle d'échange, n-aire ou non. Remplacé par une action manuelle : le propriétaire cherche lui-même une annonce `echange` compatible (`/annonces?mode=echange&tags=...`, déjà existant) et propose un échange direct à deux parties (`engine.proposeDirectSwap`, nouvelle route `POST /annonces/:id/echanger`) ; l'autre partie accepte ou refuse depuis une nouvelle page `/matchs`. Mécaniquement, une proposition manuelle est un cycle de longueur 2 — même table `match_proposals`/`match_participants`, `accept`/`refuse` inchangés. **Ce que ça change pour l'utilisateur** : plus de matching n-aire (3+ parties) possible du tout — un échange ne concerne plus jamais que deux propriétaires ; plus d'attente passive, il faut chercher activement. | **Décision utilisateur (Arthus), avertissement donné avant exécution** : le contrat verrouillé (`1-CheckList.md` M4, A6 : "matching n-cycles dès v1, pas de version allégée — Réponse client (confirmation directe)") documente une exigence confirmée par Le_Club, pas un choix dev — cette entrée renverse cette confirmation et doit être validée avec le client avant mise en production. Décision prise en connaissance de cause après question explicite posée en session (choix "remplacer complètement" plutôt que "garder le TTC en fond + ajouter la recherche manuelle en plus"). **Mise à jour 2026-09-17 : validé par Le_Club** — l'interprétation d'Arthus (retrait complet du TTC, remplacement par la proposition manuelle 1:1) a été soumise au client et confirmée. Le gap de mandat signalé par `4-Audit.md` (§4.1, "A19 non signé par Le_Club") est levé ; il ne reste plus que le gap technique (29 fichiers non committés, dont A19 lui-même) et les autres recommandations de l'audit. |
| A20 | Moment d'ouverture du thread hub (bug de logique repéré juste après A19) | Le thread privé (M5) s'ouvre désormais **à la création de la proposition**, jamais après. Avant : `trial.open()` ne créait la transaction et le thread qu'une fois les **deux** parties déjà engagées — sous l'ancien TTC, ça se justifiait (matching algorithmique, rien à négocier avant d'accepter), mais avec A19 (l'initiateur choisit délibérément son interlocuteur), forcer un accord à l'aveugle avant de pouvoir échanger un seul message n'a plus de sens. `trial.open()` scindée en deux : `createTransactions` (appelée par `engine.js` dès `matchRepo.createProposal`, quel que soit `don` ou `echange` — crée les transactions `PROPOSED` et publie `intent.hub.thread_create` immédiatement) et `confirmAccepted` (appelée seulement une fois tous les participants d'accord, ne fait plus que promouvoir en `ACCEPTED` et déclencher `intent.trial.assign`). Effet de bord positif repéré en écrivant le correctif : `dissolveProposal` restaurait les annonces à la main sans jamais regarder si une transaction existait déjà — avec A20 (transaction quasi systématiquement présente désormais), un `refuse()` arrivant après coup sur une proposition déjà `accepted` aurait pu laisser une transaction `ACCEPTED`/`TRIAL` orpheline pendant qu'on remettait l'annonce en `active` ; corrigé en délégant à `trial.cancel` (qui existe déjà, cascade déjà sur les transactions sœurs) dès qu'une transaction existe pour la proposition. | Décision utilisateur ("y'a surtout un problème de logique, le chat est ouvert après la validation de l'échange ??"), appliquée aux deux modes (`don` et `echange`) pour rester cohérent plutôt que de bricoler un cas spécial |
| A21 | Bug réel trouvé en test à deux comptes réels (post-A20) : `proposeDirectSwap` plantait en base | `listings.create()` ne considérait une guilde comme "déjà occupée" que si elle portait une annonce `active`/`pending_bot` — pas `matched`. Une annonce restée `matched` indéfiniment (proposition jamais acceptée/refusée/expirée) n'empêchait donc pas la création d'une **deuxième** annonce sur la même guilde ; les deux annonces pouvaient ensuite chacune vouloir une ligne `transactions` ouverte pour cette guilde (créée dès la proposition depuis A20), violant la contrainte `uniq_transactions_open_guild` (une seule transaction non terminale par guilde) — `DatabaseError 23505` remonté en 500 brut au clic sur "Proposer cet échange". Corrigé : `matched` ajouté aux statuts "vivants" bloquant une nouvelle annonce, à la fois côté applicatif (`listingsRepo.findLiveByGuild`) et côté base (`uniq_listings_live_guild` étendu, migration 004) — une guilde ne peut plus jamais porter deux annonces simultanément, `matched` compris, donc `uniq_transactions_open_guild` ne peut plus être violée par ce chemin. | Bug réel, trouvé en testant avec un deuxième compte Discord réel — cf. `3-DebugNotes.md` |
| A22 | Aucune invitation vers le serveur hub n'était jamais montrée aux propriétaires | Repéré après A21 : le mécanisme de thread privé (M5) ne fonctionne que pour un propriétaire déjà membre du serveur hub Discord — `thread.members.add()` échoue silencieusement sinon, et son repli (une mention postée *dans* le thread) est invisible pour quelqu'un qui ne peut justement pas voir le contenu d'un serveur dont il n'est pas membre. Rien, nulle part sur le site, ne disait à un propriétaire de rejoindre ce serveur. Corrigé : le bot crée (ou réutilise, `unique: false`) une invite permanente sur le salon de négociation au démarrage (`bot/hub.js:ensureHubInvite`, appelée depuis `ClientReady`), persistée dans `settings.hub_invite_url` ; le site l'affiche sur le tableau de bord (en permanence) et sur `/matchs` (juste au-dessus des propositions, là où c'est le plus utile). | Repéré par l'utilisateur ("aucune invitation n'a été envoyée aux comptes de propriétaires... ils n'ont donc jamais accès aux messages") — bug réel, pas un choix produit |
| A23 | Compléter A22 : notifier directement le propriétaire absent du hub, pas seulement via le site | Les propriétaires sont toujours `@` mentionnés par leur `discord_id` dès la mise en contact (`participantDiscordIds`) — autant leur envoyer directement un MP avec le lien d'invite plutôt que de compter uniquement sur le site, que tout le monde ne consulte pas forcément. `bot/hub.js:notifyMissingHubMember` : quand `thread.members.add()` échoue, en plus de la mention (déjà inutile seule, cf. A22) le bot tente un MP direct (`client.users.fetch(discordId)` puis `user.send(...)` avec le lien `hub_invite_url`) — best-effort, pas un mécanisme de chat (O2 reste valide : un MP fermé/sans serveur commun renvoie 403, auquel cas le lien affiché sur le site (A22) reste le filet de sécurité garanti). | Suggestion utilisateur ("les proprios sont @ dans le Discord. Il suffit juste de leur envoyer un MP d'invitation s'ils ne sont pas sur le serv Discord") |
| A24 | Revient sur le compromis d'A18 : `match_participants`/`listing_queue` ne doivent plus bloquer la suppression physique d'une annonce | A18 avait choisi `ON DELETE RESTRICT` en connaissance de cause (voir sa propre entrée) : une annonce ayant un historique de match ou de file d'attente retombait en soft-delete (`removed` en base) plutôt que de disparaître, pour ne jamais perdre l'historique consultable via une jointure sur `listings`. Revu : cet historique-jointure n'a jamais été le seul filet — `audit_log.before`/`after` (`listing.created`/`listing.removed`) capture déjà tout le contenu, indépendamment de la ligne `listings`, et c'est bien cette trace-là (pas la ligne elle-même) qui doit rester consultable par un futur admin. Migration 005 : les deux FK passent de `RESTRICT` à `SET NULL` (`match_participants.listing_id`, devenu nullable, `.gives_to_listing_id`, `listing_queue.listing_id`, devenu nullable) — la ligne de match/file d'attente est conservée, seule la référence pendante vers l'annonce supprimée est effacée. `domain/listings.js:remove()` n'a pas changé de logique (toujours `hardDelete` d'abord, repli soft-delete sur `23503` sinon) — ce repli devient simplement un filet purement défensif, plus le chemin normal pour ces deux tables. Nettoyage ponctuel : les lignes `removed` déjà bloquées avant cette migration (dont l'annonce "Cacahuètes et Kkhuètes" repérée par le client en test live) ont été supprimées physiquement une fois la contrainte levée. | Décision utilisateur ("Inutile de la garder dans l'historique. Or, il peut être bon de garder les créations et les suppressions - pour que des futurs admins pourront consulter"), révisant un choix qu'il avait lui-même fait sous A18 |
| A25 | A19 (échange manuel) n'avait jamais de vérification de correspondance entre annonces | Depuis A19, proposer un échange est un choix délibéré : le propriétaire trouve lui-même une annonce compatible et propose une de ses propres annonces `echange` — mais rien, ni le menu déroulant de `/annonces/:id` (déjà correct : il liste bien **toutes** les annonces `echange` actives du propriétaire, sans filtre par tags) ni `engine.proposeDirectSwap`, ne vérifiait que les deux annonces se correspondent. On pouvait proposer n'importe quelle annonce contre n'importe quelle autre, y compris sans aucun rapport de tags. L'ancien moteur TTC (retiré par A19) exigeait déjà cette correspondance par construction (`preferences.js` : un candidat sans recoupement `seekingTags`/`tags` était simplement exclu du calcul, score 0) — A19 a supprimé le calcul de cycle sans réintroduire l'équivalent pour la proposition manuelle. Corrigé dans `engine.js:proposeDirectSwap`, après la vérification de mode : rejet (`ERR_TAG_MISMATCH`) si aucune des deux directions ne se recoupe (mes `tags` ∩ ses `seekingTags`, et ses `tags` ∩ mes `seekingTags`) — la sélection reste libre en amont (menu déroulant inchangé, toutes mes annonces `echange`), seule la validation se fait en aval, côté domaine. | Demande utilisateur ("quand on consulte une annonce, on doit pouvoir proposer TOUTES les annonces en échange. La vérification de la correspondance des flags se fait en aval"), confirmée après vérification du code existant (aucun filtre nulle part, ni en amont ni en aval) |
| A26 | Une guilde Discord supprimée par son propriétaire restait affichée indéfiniment dans "Mes serveurs" / "Serveurs sans annonce publiée" | `web/auth/oauth.js:syncOwnedGuilds` (appelée au plus 1×/min/utilisateur depuis `user.js:ownedGuilds`) ne faisait qu'ajouter/rafraîchir les guildes que Discord renvoie comme possédées — jamais de réconciliation pour celles qui **disparaissent** de cette liste (guilde supprimée, quittée, ou ownership transférée hors-plateforme). `guilds.owner_discord_id` (colonne `NOT NULL`) restait donc figé sur l'ancien propriétaire pour toujours. Corrigé : après avoir confirmé les guildes actuellement possédées, `syncOwnedGuilds` compare à `guildsRepo.listOwnedByDiscordId` (l'état stocké) et désassigne (`ownership.UNOWNED`, sentinel `''` — jamais un vrai snowflake Discord, donc aucune collision possible) toute guilde qui n'apparaît plus dans la liste Discord fraîche, via le pipeline `ownership.observe()` existant (donc tracé dans `ownership_events`, source `oauth`). Guildes avec une transaction ouverte explicitement exclues de cette désassignation (`transactionsRepo.findOpenByGuild`) : le signal faisant autorité pendant un transfert en cours vient du bot (gateway/sweep), pas de la liste de guildes périmée d'un des deux utilisateurs. | Bug réel signalé par l'utilisateur ("La guilde 'Hentai <3' a été supprimée, et elle est toujours listée dans mes serveurs") — vérifié en live (Chrome + requêtes DB directes) : la guilde disparaît de `/tableau-de-bord` après resynchronisation, `owner_discord_id` passe à `''`, événement tracé dans `ownership_events` |
| A27 | Le panel admin (§2bis) n'avait jamais d'interface — 24 routes JSON brutes, aucune page HTML | `web/routes/admin.js` couvrait déjà les 9 domaines admin (utilisateurs/bans, annonces, signalements, avis, RBAC, transactions/litiges, réglages, stats, audit) côté API, mais personne chez Le_Club ne peut taper des requêtes HTTP à la main — même trou que `/matchs` avant qu'il ait une page (cf. `3-DebugNotes.md`, 2026-09-06). Donné une page HTML (navigation par défaut ; JSON conservé pour tout appelant envoyant `Accept: application/json`) à chacune des 24 routes, formulaires `<form>` classiques sans JS (CSP `script-src 'self'` sans `unsafe-inline`, cf. M8), CSRF partout. Deux bugs bloquants trouvés et corrigés en construisant cette UI, tous deux dans le chemin critique de la modération : (1) `listings.hide`/`restore` exigeaient que l'acteur soit le **propriétaire Discord observé de la guilde** (`ownership.assertOwnershipForListing`) alors que ces deux fonctions ne sont appelées que par le panel admin, jamais en self-service — un modérateur qui n'est pas propriétaire de la guilde du contrevenant se serait pris `ERR_NOT_OWNER` en boutonnant "Masquer" ; remplacé par une vérification RBAC (`listings.hide`), cohérente avec `moderation.sanction`/`rbac.assignRole` etc. (2) Trois guards de route ne correspondaient pas à la permission vérifiée côté domaine (`/admin/users/:id/sanction`+`/admin/sanctions/:id/lift` gardés par `users.ban` mais `moderation.sanction` vérifié dans `moderation.js` ; `/admin/users/:id/roles` gardé par `users.manage_roles` mais `rbac.grant` vérifié dans `rbac.js`) — invisible avec le rôle `moderateur`/`administrateur` seedés (qui ont les deux ensemble) mais aurait cassé silencieusement tout rôle personnalisé plus granulaire ; guards réalignés sur la permission réellement enforced côté domaine. Complément : `listingsRepo.listPublic` (utilisé jusqu'ici par `GET /admin/listings`) force `status = 'active'` en dur (c'est la vitrine publique) — impossible d'y retrouver une annonce `hidden` pour la restaurer. Nouvelle méthode dédiée `listingsRepo.listForModeration` (statut optionnel, pas de filtre forcé), sans toucher à `listPublic`/`buildListPublicWhere` (couverts par le test EXPLAIN de régression). | Demande utilisateur ("Panel admin stp"), après question explicite sur l'ampleur (parité complète choisie plutôt qu'un MVP partiel). Vérifié en live dans Chrome avec un compte réel promu `proprietaire` en base pour le test : hide/restore, sanction/levée, attribution/retrait de rôle, réglages, filtre annonces/signalements, audit, stats — tous testés manuellement, plus test (6) + test:db (15) au vert. Un bug de CSP trouvé et corrigé pendant ce test live : un `onchange="this.form.submit()"` sur les filtres de statut (annonces, signalements) est silencieusement bloqué par `script-src 'self'` — remplacé par un bouton "Filtrer" visible, comme le reste du site. Gaps restants, non corrigés (hors scope de cette demande, notés pour plus tard) : aucune recherche d'utilisateur autonome dans l'API (on n'atteint une fiche `/admin/users/:id` que via un signalement/une annonce/l'audit) ; aucun mécanisme de bootstrap pour le tout premier `proprietaire` (`rbac.assignRole` exige déjà `rbac.grant`, donc déjà avoir un rôle — testé ici via un `INSERT` SQL direct). |
| A28 | Moteur de recherche léger — utilisateur/alias/serveur/tag — panel admin **et** site public | Comble le premier gap noté sous A27. Nouvelle méthode partagée `listingsRepo.search(tx, term, { status, limit })` : 4 branches indexées (`tags @> ...` sur le GIN existant, nom de guilde et pseudo propriétaire via deux nouveaux index trigram `pg_trgm`, description via le GIN full-text existant) réunies par `UNION` plutôt qu'un seul `WHERE` à coups de `OR` — un `OR` mélangeant GIN array/trigram×2/full-text aurait poussé Postgres vers un Seq Scan sur au moins une branche ; un `UNION` laisse le planner choisir le meilleur index par branche indépendamment, et déduplique gratuitement une annonce qui matche sur plusieurs branches à la fois. Pas de pagination par curseur : un top-N plat (comme n'importe quelle recherche), pas un browse. Migration 006 : `pg_trgm` + `idx_guilds_name_trgm`/`idx_users_username_trgm` (recherche) + `idx_listings_guild_id`/`idx_listings_user_id` (aucun des deux FK n'était indexé — sans eux, les branches serveur/propriétaire retombaient sur un Seq Scan de `listings` entier pour rejoindre les quelques lignes trouvées côté `guilds`/`users`, découvert par le test EXPLAIN dédié). Admin (`GET /admin/search`, lien depuis `/admin`) : utilisateurs (pseudo, UUID exact, Discord ID exact), serveurs (nom, snowflake exact — chaque résultat renvoie vers `/admin/listings?guildId=...`, nouveau filtre sur cette route), annonces (tag/serveur/propriétaire). Public (`GET /annonces?search=...`, formulaire sur `/` et `/annonces`) : mêmes 4 critères sur les annonces `active` uniquement. **Décision produit prise en session** : exposer publiquement le nom du serveur et le pseudo du propriétaire — jusqu'ici, aucune page publique ne montrait le nom d'une guilde Discord (même la fiche d'une annonce ne montrait que sa taille), et rien ne reliait une annonce à un profil `/u/:id`. Question posée explicitement avant de coder (rester sur tags/description existants vs exposer nom de serveur + pseudo) : choix confirmé d'exposer — pas une exigence client verrouillée en Phase I (vérifié dans ce fichier et `2-Architecture.md`, rien trouvé), donc une décision produit du dev en session, pas une reversal nécessitant validation Le_Club, mais documentée ici pour la même raison que toute bascule de visibilité de données. `listingsListHtml`/la fiche annonce affichent maintenant systématiquement serveur + propriétaire (cohérence : cacher cette info dans le browse par défaut tout en la montrant en recherche aurait été arbitraire). | Demande utilisateur ("Ajoutons également un moteur de recherche léger, permettant de chercher un utilisateur, son alias, un serveur ou un tag"), ampleur clarifiée par question explicite (panel admin seul vs + recherche publique — choix : les deux), puis question sur l'exposition de données publiques avant d'implémenter la partie publique (choix : exposer). Vérifié en live dans Chrome (recherche publique par pseudo propriétaire et par snowflake de guilde en admin, filtre `/admin/listings?guildId=`, fiche annonce publique affichant serveur+propriétaire) + test (6) + test:db (19, dont 4 nouveaux tests EXPLAIN dédiés) au vert. Bug trouvé et corrigé en cours de route : `= ANY(tags)` n'est pas indexable par le GIN `array_ops` existant (seuls `&&`/`@>`/`<@`/`=` le sont) — dégénérait en Seq Scan ; corrigé en `tags @> ARRAY[...]`. |
| A29 | Précision sur A28 : "flags" = tags portés par le serveur, via son annonce vivante actuelle | Clarifié par questions successives (interprétation "flags booléens `bot_present`/`audit_blind`/`role_hierarchy_ok`" écartée). La capacité demandée existait déjà : `listingsRepo.search` (A28) trouve un serveur par le(s) tag(s) exact(s) de son annonce `active`/vivante — un serveur sans annonce vivante, ou dont l'annonce a été retirée/masquée/matched, redevient introuvable par ce biais (lecture confirmée explicitement, pas d'historique cumulé ni de tags indépendants sur `guilds`). Rien de nouveau côté requête/index. Seule la présentation manquait de clarté côté admin : les résultats "Annonces" de `/admin/search` ne montraient ni le nom du serveur (seul le `guildId` brut était présent, comme cible de lien) ni les tags ayant matché — corrigé (`GET /admin/search` batch-fetch désormais les guildes des résultats via `guildsRepo.findByIds`, affiche le nom de serveur en tête de chaque ligne suivi des tags de l'annonce). Le côté public (`listingsListHtml`, A28) affichait déjà le nom de serveur sur chaque résultat, rien à changer là. | Demande utilisateur ("Permets également la recherche de flags liés aux serveurs"), sens précisé après une première hypothèse incorrecte (flags booléens de `guilds`) puis une deuxième question sur la source des tags (annonce vivante actuelle choisie, plutôt qu'historique cumulé ou nouveau champ indépendant). Vérifié en live : recherche admin par tag (`jeux-video`) affiche maintenant le nom de chaque serveur trouvé avec ses tags ; recherche publique par le même tag confirmée limitée aux annonces `active` (2 résultats vs 7 côté admin, qui voit tous statuts). Lint + test (6) au vert ; pas de changement de schéma/requête, test:db déjà couvert par A28. |
| A30 | La recherche publique (A28) ne remontait jamais un utilisateur directement — seulement des annonces où il apparaît comme propriétaire | `GET /annonces?search=` n'avait pas de section "Utilisateurs" comme `/admin/search` en a une (A28) : chercher un pseudo ne trouvait que les annonces de cette personne, jamais un lien direct vers sa fiche publique `/u/:id`. Corrigé : réutilise `usersRepo.searchByUsername` (déjà écrite pour l'admin, A28 — aucune nouvelle requête SQL) pour ajouter une section "Utilisateurs" au-dessus des résultats d'annonces. Volontairement plus étroit que côté admin : pas de correspondance exacte par UUID ou Discord ID (un visiteur public ne connaît ni l'un ni l'autre, et le `discord_id` reste caché — inchangé depuis `/u/:id`), seulement le pseudo, substring, via l'index trigram déjà en place. Aucun changement de schéma/index/requête — uniquement le câblage d'une méthode existante dans une deuxième route. | Demande utilisateur ("Ajoute également la recherche d'users"), suite directe de A28/A29 — parité avec la section "Utilisateurs" déjà présente côté admin, qui manquait côté public. Vérifié en live : recherche "swap-cancel" sur `/annonces?search=` affiche 4 utilisateurs (liens vers `/u/:id`) au-dessus des annonces ; clic sur un profil confirmé fonctionnel. Lint + test (6) au vert ; pas de nouveau test:db (méthode déjà existante, aucune requête SQL nouvelle). |
| A31 | Le profil public (`/u/:id`) ne montrait ni les serveurs d'un utilisateur ni ses annonces | Ajouté : une section "Serveurs" listant les annonces `active` de l'utilisateur (nom de guilde, lien vers l'annonce, mode). Filtré à `active` uniquement — même règle de visibilité que la vitrine publique et la recherche (A28/A30) : une annonce `hidden`/`removed`/`matched`/`pending_bot` n'est déjà visible nulle part ailleurs publiquement, le profil ne doit pas être la première fuite. `listingsRepo.listByUser` (partagée avec le tableau de bord, l'export RGPD, et la modération — tous ont besoin de tous les statuts) gagne un filtre `status` optionnel plutôt qu'une nouvelle méthode dédiée, backward-compatible (tous les appelants existants passent toujours sans statut). Noms de guilde batch-fetchés via `guildsRepo.findByIds` (même pattern qu'A28/A29), pas de N+1. | Demande utilisateur ("Sur les profils des users, montre leurs serveurs stp"), suite directe d'A28-A30. Vérifié en live : profil avec une annonce active affiche "Serveurs (1)" avec le nom de la guilde ; profil sans annonce active affiche l'état vide ("Aucune annonce active pour l'instant") plutôt qu'une section vide silencieuse — vérifié sur un compte dont l'unique annonce est `matched` (donc correctement absente de cette liste). Lint + test (6) + test:db (19, `listByUser` étant partagée avec du code déjà couvert) au vert. |
| A32 | Aucun lien nulle part sur le site ne menait au panel admin — seuls ceux qui connaissaient déjà `/admin` pouvaient l'atteindre | Le panel lui-même dégrade déjà proprement pour un compte sans capacité RBAC (l'index ne montre que les sections autorisées, vide si aucune) — mais rien sur `/tableau-de-bord` ne signalait même l'existence de la page à un admin. Corrigé : lien "Panel admin" ajouté à la barre de navigation du tableau de bord (`web/routes/user.js`), affiché uniquement si `req.caps.size > 0` (déjà résolu par `requireAuth` sur cette route, aucune requête supplémentaire). Toujours pas de mécanisme de self-service pour devenir le premier admin — un rôle s'attribue toujours par `INSERT` SQL direct ou depuis la fiche d'un utilisateur *déjà* admin (`rbac.assignRole` exige `rbac.grant`) ; gap noté sous A27, toujours non résolu, hors scope de cette demande précise. | Demande utilisateur ("surtout, pk y'a rien pour y acceder ? Donnes un acces, via le profil"), accompagnant une bascule du rôle `proprietaire` de test de `dy_epic` vers `arde_ass` (`user_roles`, `DELETE`+`INSERT` directs — même limite de bootstrap qu'A27/A28). Vérifié en live dans Chrome : `arde_ass` (rôle `proprietaire`) voit "Panel admin" sur son tableau de bord et atteint `/admin` avec toutes les sections visibles ; `dy_epic` (plus aucun rôle après la bascule) n'a plus la ligne en base — comportement du lien conditionnel non re-testé visuellement faute de session Discord pour ce compte dans l'onglet Chrome de la session, mais la condition (`caps.size > 0`) et la dégradation de l'index admin pour un compte sans capacité étaient déjà couvertes par les tests live d'A27. Lint + test (6) au vert. |

| A33 | `2bis Transactions` n'avait pas de route de liste côté admin — recommandation #4 de `4-Audit.md` | Seule route jusqu'ici : `GET /admin/transactions/:id`, atteignable uniquement en connaissant déjà l'UUID (une notification, un signalement, l'audit). Le domaine le plus sensible du panel (litiges/transferts) était aussi le moins navigable. Corrigé : nouvelle méthode `transactionsRepo.listForModeration(tx, { status, guildId }, page)` (même schéma que `listingsRepo.listForModeration`/A27 : filtres optionnels, pas de scope forcé, contrairement à `listByUser`) ; nouvelle route `GET /admin/transactions?status=&guildId=` (table, filtre statut, lien vers le détail de chaque ligne) ; lien "Transactions" ajouté à l'index `/admin` (manquait aussi, même trou que A32 pour le panel lui-même). | Demande utilisateur ("Execute tout"), après audit externe (`4-Audit.md`, §3/§4.3) ayant trouvé ce gap en lisant le code directement plutôt qu'en supposant. Vérifié en live : `/admin/transactions` liste les 32 transactions de la base de dev, filtre par statut testé (`TRANSFERRED` → 9 résultats), pagination et lien "Détail" fonctionnels, "Transactions" visible depuis l'index `/admin`. Lint + test (6) + test:db (19) au vert. |
| A34 | Comment un échange se valide réellement ? Trois trous : pas d'invitation vers la guilde cible, pas de rappel de fin d'essai, pas de message de clôture | Le vrai bloquant, trouvé en lisant `bot/trialRole.js` plutôt qu'en supposant : `onIntentTrialAssign` faisait `guild.members.fetch(memberDiscordId)` et, si absent, abandonnait avec une seule ligne d'audit (`ERR_MEMBER_NOT_IN_GUILD`) — jamais de retry, jamais d'invitation. Deux inconnus mis en contact par le marketplace n'ont pourtant aucune raison d'être déjà membres l'un du serveur de l'autre : la transaction restait bloquée à `ACCEPTED` **pour toujours**, sans signal visible pour personne. Corrigé en quatre volets, demandés ensemble : **(1) Invite + retry** — le bot génère une invite vers la guilde cible et la DM (+ message dans le thread hub en repli, A23) la première fois que `ERR_MEMBER_NOT_IN_GUILD` survient (`invite_sent_at`, un seul essai, best-effort comme le reste des notifications Discord de ce projet) ; `jobs.acceptedInviteRetryTick` (10 min, REST uniquement) revérifie ensuite si le destinataire a rejoint et refait `intent.trial.assign` si oui. **(2) Rappel J-1** — `jobs.trialReminderTick` (horaire) publie `intent.trial.reminder` pour toute transaction `TRIAL` dont `trial_ends_at` tombe dans les 24h, jamais deux fois (`trial_reminder_sent_at`) ; posté dans le thread hub. **(3) "Échange terminé"** — `bot/hub.js:onEventTransactionUpdated` distingue maintenant `TRANSFERRED` (✅) et `EXPIRED` (❌) du statut brut affiché pour tout le reste, avec un message de clôture identique dans l'esprit (c'est fini) mais expliquant pourquoi. **(4) Durée par défaut 7 -> 3 jours** — `trial_duration_days` (réglage global, pas négocié par échange, choix explicite de l'utilisateur). Migration 007 : deux colonnes `transactions.invite_sent_at`/`trial_reminder_sent_at`, mise à jour du réglage. `BOT_INVITE_PERMISSIONS` (oauth.js) gagne `Create Instant Invite` — absente sur toute guilde déjà invitée avant ce changement, tant que son propriétaire ne relance pas l'invitation. **Trois bugs supplémentaires trouvés en testant en conditions réelles, aucun lié au code d'A34 lui-même :** (a) `buildBotInviteUrl` n'a jamais demandé le scope `applications.commands` — l'enregistrement de `/signaler` échoue en 403 sur toute guilde invitée via ce flux, y compris la guilde hub, et ça faisait planter **tout le process bot** puisque `registerCommands()` n'était pas isolé de `client.login()` ; corrigé des deux côtés (scope ajouté pour les futures invites, échec de `registerCommands` rendu non-fatal). (b) `hub.js:ensureHubInvite` laissait `client.guilds.fetch()` hors de son propre `try/catch` — si la guilde hub devient injoignable (constaté en direct : le bot n'y est plus, cause non élucidée, indépendante de ce chantier), l'erreur remontait non catchée et bloquait `bus.start()` de s'exécuter **du tout** : le bot restait connecté à la gateway mais ne traitait plus aucun intent, silencieusement. Corrigé (try/catch étendu + `main.js` isole aussi chaque étape du `ClientReady`, même filet que le reste du fichier). (c) Le pool de connexions du process `bot` (max 5, valeur du contrat d'architecture) suffisait à provoquer un deadlock complet dès qu'une seule action domaine cascadait sur plusieurs canaux à la fois (`trial.expire`/`cancel` avec un seul sibling suffit) — chaque handler du bus rouvre sa/ses propre(s) connexion(s) en plus de celle déjà tenue par `processRow` pour toute la durée de l'appel, et 2-3 événements concurrents épuisaient déjà les 5 connexions disponibles. Un risque latent d'avant A34, jamais déclenché par la suite de tests existante. Relevé à 10 (comme `web`). | Demande utilisateur ("Comment suis-je censé valider l'échange ?", puis les 4 points détaillés ensemble, "4 a la fois"). Vérifié en live (Chrome + scripts ponctuels contre la base de dev, bot et jobs relancés à chaque étape) : `intent.trial.assign` consommé proprement pour une vraie transaction `ACCEPTED` sur une guilde réelle où le bot est présent (`invite_sent_at` posé, `ERR_MEMBER_NOT_IN_GUILD` tracé) ; `intent.trial.reminder` et le passage `EXPIRED` consommés proprement après un forçage de `trial_ends_at`/expiration manuelle. **Mise à jour 2026-09-17, après réinvitation du bot sur "Xyro Market Hub"** (§ note de fin de section) : le contenu exact des messages de rappel et de clôture a été confirmé visuellement en relisant, via l'API Discord, le vrai fil hub d'une vraie transaction (`4d426732-...`, comptes `arde_ass`/`dy_epic`) — les trois textes (⏰ rappel, ❌ caduque, ✅ transféré) apparaissent correctement formatés dans Discord, date localisée en `fr-FR` comprise. Le message d'invitation n'a pas été revérifié séparément dans cette passe mais partage strictement le même mécanisme d'envoi (`thread.send` sur le même client). Lint + test (6) + test:db (21, dont 2 nouveaux tests dédiés à `findAcceptedAwaitingTrial`/`findTrialsEndingSoon`) au vert. |

| A35 | "Il ne manque plus que le CSS" — reprendre les codes visuels de Discord/Disboard, simple ; puis "fetch les infos des serveurs et des users pour rendre ça plus joli" | Le site n'avait jamais eu de CSS — HTML nu, zéro `class`. Ajouté un unique `src/web/public/style.css` (servi via `/static/`, déjà en place pour ça depuis le début — CSP `style-src 'self'` déjà configurée, `imgSrc` autorisait déjà `cdn.discordapp.com`) lié une seule fois dans `render.js:layout()` — s'applique à toutes les pages sans toucher aux routes, puisqu'il n'y a jamais eu qu'un seul template partagé, pas un fichier HTML par page. Thème sombre inspiré de Discord (palette blurple `#5865F2`, fond `#313338`, police système `-apple-system, Segoe UI...`) + listes en "cartes" façon Disboard. Puis icônes de serveur et avatars utilisateur réels : `guildIconUrl`/`userAvatarUrl`/`guildIconHtml`/`userAvatarHtml` (nouveaux, `render.js`) construisent les URLs CDN Discord (`cdn.discordapp.com/icons/…`, `/avatars/…`, repli sur l'avatar par défaut Discord si absent) ; câblés sur les listes d'annonces publiques, la fiche annonce, le profil public, la recherche et le tableau de bord. **Quatre bugs réels trouvés en le faisant :** (1) 15 occurrences de `style="display:inline"` bloquées silencieusement par la CSP (`style-src 'self'`, sans `unsafe-inline`) depuis le tout début du projet — remplacées par une classe `.inline` ; (2) `guilds.icon_hash` existait dans le schéma depuis toujours mais n'était écrit nulle part (`guildWatcher.js`/`oauth.js` ne le passaient jamais à `updatePresence`/`ensureExists`) — corrigé aux deux sources (gateway + OAuth, cette dernière étant la seule à voir une guilde `pending_bot` avant que le bot ne la rejoigne) ; (3) `userAvatarUrl` plantait (`BigInt()`) sur un `discord_id` de fixture de test non-numérique affiché en propriétaire d'annonce sur la page d'accueil (500 en direct) — corrigé par une regex snowflake avant tout `BigInt()`, repli sur l'avatar par défaut sinon ; (4) `/static/*` est servi `immutable, max-age=30j` (`web/main.js`, déjà en place, adapté à un futur asset avec hash mais pas à `style.css` édité à la main) — sans cache-buster, un visiteur ayant déjà chargé une version de la CSS ne revoyait jamais la suivante avant 30 jours ; corrigé par un hash de contenu SHA-1 calculé une fois au boot (`STYLE_VERSION`), ajouté en `?v=` sur le lien — invalidation automatique à chaque édition du fichier, pas de version à penser à incrémenter. Tables admin nettoyées de `border="1" cellpadding="4"` (rendu 100% CSS désormais). Admin non touché par les icônes/avatars (délibéré — tables denses, pas le rôle de l'admin d'être "joli"). | Demande utilisateur, en deux temps ("Il ne manque plus que le CSS..." puis "Fetch les infos des serveurs et des users pour rendre ça un peu plus joli"). Question posée en cours de route ("un index.html par page, non ?") — réponse : non, un seul template serveur partagé existait déjà, un seul fichier CSS suffit. Vérifié en live dans Chrome (accueil, tableau de bord, panel admin, fiche annonce, profil public arde_ass/dy_epic — avatar réel de dy_epic confirmé affiché, repli lettre coloré confirmé sur les guildes de test sans icône, repli avatar Discord par défaut confirmé sur les comptes de test sans avatar) + aucune erreur console/CSP après hard-reload. Lint + test (6) + test:db (21) au vert. |

| A36 | Suite d'A35 : "Pas fan du design... reprends des vrais éléments de Discord" (captures du profil Discord et de la page Découvrir fournies), puis "sur toutes les pages, si possible", puis "ajoute le chargement de la bannière" et "simule des avis, pour voir" | Trois volets, mêmes captures de référence tout du long. **(1) Vraies cartes Discord Découvrir** : `render.js:cardHtml` (nouveau, remplace les `<li>` texte) — bannière dégradée, icône du serveur qui mord dessus (`margin-top` négatif + bordure de la couleur du fond), titre, description tronquée sur 2 lignes, pied de carte (pastille de mode + avatar propriétaire). `listingCardHtml` en devient un simple appelant. Appliqué partout où une liste d'annonces/serveurs est parcourue : accueil, `/annonces`, recherche, profil public, **et le tableau de bord** (3 listes : échanges en cours, serveurs sans annonce, mes annonces — chacune avec ses propres actions dans le pied de carte, ex. `Modifier`/formulaire `Supprimer`). Décision assumée : pas de rail de serveurs ni de liste de salons à la Discord — ce site reste un marketplace public avec sa propre structure de pages, seul le vocabulaire visuel (cartes, bannière+icône, pills, badge vérifié) est repris, pas le chrome de l'appli. Tags devenus des pills (`pillHtml`), badge "✓ Vérifié" en pastille verte (`verifiedBadgeHtml`), profil public refait en "popout" Discord (bannière + avatar qui déborde dessus, `.profile-header`/`.profile-identity`). **(2) Chargement de la vraie bannière Discord** (serveur et utilisateur), pas juste le dégradé de repli — migration 008 (`users.banner_hash`, `guilds.banner_hash`, `guilds.splash_hash`), captés aux mêmes sources que l'avatar/l'icône (A35) : `oauth.js` pour l'utilisateur (`me.banner`, scope `identify` déjà suffisant), `guildWatcher.js` pour la guilde (`guild.banner`/`guild.splash` — uniquement disponibles une fois le bot dans la guilde, l'endpoint liste-de-guildes OAuth ne les renvoie pas) ; `splash_hash` sert de repli pour `banner_hash` côté guilde, un vrai "banner" Discord exigeant un palier de boost que peu de petits serveurs atteignent, contrairement au splash (fond de la page d'invite). `render.js:bannerUrl`/`guildBannerUrl`/`userBannerUrl`/`bannerImgHtml` — même endpoint CDN `/banners/{id}/{hash}` pour les deux types. **(3) Avis simulés + enfin affichés** : la page de profil public n'avait jamais listé le moindre avis individuel, seulement l'agrégat brut ("Avis : 0 (moyenne N/A)") — `reputation.history` existait déjà (utilisé par l'admin) mais n'était câblé nulle part côté public. Ajouté : section "Avis" sur `/u/:id` (10 derniers, `reviewHtml`/`starsHtml` — étoiles en caractères unicode, pas d'image, rien à charger). 5 avis de test insérés en SQL direct dans `xyro_dev` pour la démo (5 faux comptes reviewers, 5 fausses transactions `TRANSFERRED` vers `dy_epic`) — **données de dev uniquement, aucune migration, ne fait partie d'aucun commit**, comparable à la pollution `seed-guild-*` déjà notée ailleurs dans ce fichier. | Demande utilisateur en plusieurs temps, avec deux captures d'écran du vrai client Discord (popout de profil, page Découvrir) fournies comme référence explicite pour "faire mieux". Vérifié en direct dans Chrome à chaque étape : accueil/annonces/recherche/profil en grille de cartes, tableau de bord entièrement en cartes (formulaire Supprimer confirmé fonctionnel dans un pied de carte flex-wrap), bannière animée réelle d'`arde_ass` chargée après une reconnexion OAuth ciblée (`GET /users/{id}` via le bot confirmant au préalable que `dy_epic` et les guildes de test n'ont, eux, aucune bannière/splash réelle — repli dégradé confirmé correct dans ce cas), page de profil de `dy_epic` affichant "★★★★☆ 5 avis (moyenne 4.4/5)" et les 5 avis simulés avec étoiles/date/texte. Aucune erreur console/CSP. Lint + test (6) + test:db (21) au vert. |

| A37 | Retour sur A36 (capture d'écran du rendu réel fournie) : "Pas besoin de faire si large. Et la photo doit être par dessus la bannière. Idem pour les serveurs. Et, pour les serveurs, si il n'y a pas de bannière, mets une couleur correspondant à la moyenne des couleurs de la photo du profil" | Deux corrections de mise en page + une fonctionnalité. **Bannières réduites et recouvrement augmenté** : `.profile-banner` 90px -> 56px, avatar 84px -> 72px, chevauchement sur la bannière porté à ~64 % de la hauteur de l'avatar (`margin-top` -46px) au lieu de ~40 % — même traitement sur `.card-banner`/`.card-icon` (64px -> 44px, icône 52px -> 44px, chevauchement ~64 %) pour que l'icône de serveur "repose" visiblement sur la bannière plutôt que de simplement l'effleurer par en-dessous. **Couleur moyenne de l'icône en repli de bannière** (uniquement pour les serveurs, pas pour le profil utilisateur — lu explicitement dans la demande) : nouveau module `web/iconColor.js`, dépendance `pngjs` ajoutée (décodeur PNG pur JS, sans binaire natif — le VPS ciblé fait 1 Go de RAM, `sharp` et son `libvips` natif étaient hors de question) — télécharge l'icône du serveur en 32px, force `.png` même si le hash est animé (Discord sert la première frame en statique sur cette extension, évite d'écrire un second décodeur GIF pour une valeur qui finit de toute façon réduite à une seule couleur), moyenne les pixels non transparents, met en cache par `guildId:iconHash` (durée de vie du process). Le résultat ne peut pas passer par un `style="background:…"` (CSP `style-src 'self'`, aucun attribut `style` inline nulle part dans ce projet) : rendu à la place comme une image `data:image/svg+xml` 1×1 de cette couleur (`render.js:solidColorImgHtml`), ce qui a nécessité d'ajouter `data:` à la directive `img-src` du CSP (`web/main.js`) — strictement limité aux images, jamais aux scripts/styles. Câblé partout où `cardHtml` reçoit une guilde : `lookupMaps` (accueil/`/annonces`/recherche), `/u/:id`, et les 3 listes du tableau de bord. **Non vérifiable visuellement avec les données actuelles** : aucune des guildes de test réelles (`Cacahuètes et Kkhuètes`, `Xyro Market Hub`) n'a d'icône définie sur Discord (confirmé via `GET /guilds/{id}` : `icon: null` sur les deux) — le mécanisme de décodage/moyenne a été vérifié isolément contre une vraie image publique du CDN Discord (avatar par défaut #5, 256×256, décodé et moyenné avec succès en `#ef69b1`, cohérent avec son dégradé rose/magenta) et la sortie HTML de `cardHtml` vérifiée séparément (URL `data:` correctement construite) — mais personne n'a encore vu la couleur s'afficher sur une vraie carte, faute d'icône de test disponible. À revérifier une fois qu'une guilde de test aura une icône. | Demande utilisateur avec une capture d'écran du rendu réel du site (pas une référence Discord cette fois) pointant précisément les deux défauts de mise en page. Layout vérifié en direct dans Chrome (profil `arde_ass` avec sa vraie bannière, grille de cartes sur l'accueil) — chevauchement bannière/photo nettement plus proche de la référence Discord d'A36. Repli couleur-moyenne non vérifiable en direct (raison ci-dessus), mécanisme sous-jacent testé isolément. `npm audit` signale une vulnérabilité haute préexistante et sans rapport (`@fastify/static` <=10.1.1, path traversal/bypass — signalée à l'utilisateur, pas corrigée ici, changement cassant hors du périmètre de cette demande). Lint + test (6) + test:db (21) au vert. **Complément same-thread** : "il faudrait également ajouter la visualisation du profil sur le tableau de bord" — le tableau de bord affichait juste "Bonjour {pseudo}" en texte brut ; remplacé par le même `.profile-header` que `/u/:id` (bannière réelle, avatar, avis agrégés, lien "Voir mon profil public") — `req.user` (session) est volontairement minimal (id/discordId/username/isVerified, pas d'avatar/bannière), donc `usersRepo.findById` + `reputation.aggregate` ajoutés au `Promise.all` de la route pour obtenir la ligne complète. Vérifié en direct : bannière animée réelle d'`arde_ass` affichée sur son propre tableau de bord. **Retour supplémentaire, capture du popout Discord réel à l'appui** : "Reprends ces proportions... les bannières ne doivent pas être crop." Cause : `object-fit: cover` sur une hauteur fixe (56px profil / 44px carte) découpait toute image dont le ratio ne correspondait pas exactement à ce cadre — visible en direct sur la vraie bannière d'`arde_ass`. Corrigé en profondeur plutôt qu'en surface : `.profile-banner`/`.card-banner` passent d'une hauteur fixe à `aspect-ratio: 5/2` (proportion réelle d'une bannière Discord) et `.banner-img` passe de `cover` à `object-fit: contain` — l'image entière reste **toujours** visible, quitte à laisser le dégradé/la couleur de repli apparaître sur les bords si le ratio source ne correspond pas exactement (jamais de découpe, jamais de bande noire). `.profile-header` gagne aussi `max-width: 400px` (n'était pas dans la demande au sens strict, mais nécessaire : sans carte resserrée, une bannière au ratio 5:2 sur la pleine largeur de `main` (1080px) aurait fait ~430px de haut, contredisant "pas besoin de faire si large" du tour précédent) — proportion finale très proche de la capture Discord fournie (carte étroite, bannière large mais compacte, avatar qui déborde nettement dessus). Même traitement sur les cartes serveur. | Capture du rendu réel du site (pas une référence Discord) montrant précisément la bannière recadrée. Vérifié en direct dans Chrome après hard-reload : profil `arde_ass` (dashboard et `/u/:id`) — bannière animée entièrement visible, dégradé bleu visible sur les côtés là où le ratio ne colle pas exactement, carte nettement plus proche des proportions de la capture Discord fournie ; grille de cartes serveur idem. Aucune erreur console. Lint + test (6) + test:db (21) au vert. **Correction immédiate** : "Le rendu est affreux. Contente-toi de crop juste autant que le fait Discord... je t'ai même fourni les dimensions" — `object-fit: contain` a été un contresens : la demande précédente ("ne doivent pas être crop") visait le **découpage excessif** dû à un ratio de conteneur arbitraire, pas l'absence totale de recadrage — `contain` produisait des bandes de dégradé sur les côtés, pas ce qui était demandé. Mesure exacte de la zone de recadrage propre à Discord effectuée directement sur la capture fournie (outil "Modifier l'image" du profil), par script (`pngjs`, détection des bords blancs de la fenêtre de recadrage) plutôt qu'à l'œil : 357×123px -> ratio 2,90:1 (pas 2,5:1 utilisé précédemment). `object-fit` repassé à `cover` sur ce ratio corrigé — le site recadre désormais exactement comme le ferait Discord, ni plus ni moins. | Capture de l'outil de recadrage natif de Discord fournie explicitement comme référence dimensionnelle. Vérifié en direct après hard-reload : bannière réelle d'`arde_ass` remplit maintenant tout le cadre sans bande latérale, recadrage propre et cohérent partout (profil, dashboard, cartes serveur). Lint + test (6) + test:db (21) au vert. |

| A38 | "Mets la barre de recherche en haut, dans la barre du Xyro Market. Il y aura également un bouton avec le profil, qui semblera l'afficher mais ouvrira un menu sandwich... Mon profil / Mes annonces / Se déconnecter" | En-tête global refait : recherche + compte deviennent des éléments du `<header>` (`render.js:layout`), plus une paragraphe ad hoc par page comme avant — cohérent sur tout le site, y compris les pages jusque-là non authentifiées (légales, 404). **Menu profil sans JS** : `<details>`/`<summary>` (aucun script nulle part dans ce projet, CSP `script-src 'self'` sans `unsafe-inline` — c'est le seul mécanisme natif, accessible au clavier, pour un menu déroulant sans JS) — l'avatar sert de `<summary>` (semble juste "afficher le profil", comme demandé), le panneau contient "Mon profil" (`/u/:id`), "Mes annonces" (`/tableau-de-bord`), "Se déconnecter" (`/auth/logout`, jamais protégé par CSRF — pas une nouvelle exigence inventée ici). **Recherche préremplie** : `layout()` accepte un `searchQuery` optionnel, propagé depuis `/annonces?search=`, pour que la barre d'en-tête affiche ce qui vient d'être cherché plutôt qu'un champ vide après validation. Formulaires de recherche redondants retirés (accueil, `/annonces` sans requête, résultats de recherche) — un seul champ, dans l'en-tête, partout. **Changement d'architecture nécessaire, pas juste visuel** : `layout()` ne connaissait jusqu'ici aucun utilisateur — chaque route construisait son propre bout de HTML "connecté en tant que X" à la main. `session.tryAuth` (déjà utilisé au cas par cas) posé comme hook global sur le seul plugin `publicRoutes` (`app.addHook('preHandler', ...)`, scope encapsulé Fastify — ne touche ni `user.js` ni `admin.js`, déjà couverts par leur propre `requireAuth`, ni `/static`/`/healthz`, hors de ce plugin) : chaque route publique reçoit désormais `req.user` sans avoir à le demander, et `user: req.user` est passé à chaque appel `layout()`/`legalPage()`/`notFoundPage()` (17 sites dans `admin.js` via `adminLayout`, 7 dans `user.js`, 7 dans `public.js`). **Bug introduit puis corrigé en le faisant** : un remplacement global (`sed`) pour ajouter `user: req.user,` à chaque appel `adminLayout({` a aussi touché la *définition* de la fonction elle-même et celle de `renderFormError` (aucun `req` dans leur portée) — repéré en relisant le diff avant de tester, pas en production ; corrigé en donnant à `adminLayout`/`renderFormError` un paramètre `user` explicite comme dans `user.js`, plutôt que de référencer un `req` qui n'existe pas dans ces portées. **Effet de bord découvert** : les pages légales passaient de `Cache-Control: public, max-age=3600` à un contenu désormais dépendant de la session (avatar affiché) — caching public d'une heure serait devenu un vrai risque de fuite (le header d'un visiteur connecté servi en cache à un visiteur anonyme suivant, via un futur proxy/CDN) ; repassé en `private, max-age=0` comme le reste du site, texte légal lui-même inchangé. | Demande utilisateur explicite, avec le comportement voulu du menu décrit précisément ("semblera l'afficher mais ouvrira un menu"). Vérifié en direct dans Chrome : recherche depuis l'en-tête sur `/`, `/annonces`, une page légale, le tableau de bord et l'admin — cohérent partout ; menu déroulant testé (ouverture, 3 liens visibles, avatar surligné quand ouvert) ; recherche testée bout en bout (`dy_epic` → résultats corrects, champ resté rempli) ; déconnexion testée en vrai depuis le menu (retour à l'état "Se connecter") puis reconnexion Discord réelle pour restaurer la session ; une page légale confirmée montrant l'avatar (preuve que le hook global fonctionne même sans middleware par-route) ; un 404 admin (`adminLayout` appelé en une ligne) et une page de détail admin (`adminLayout` multi-lignes) tous deux confirmés sans erreur. Aucune erreur console à aucune étape. Lint + test (6) + test:db (21) au vert. |

## 6. Étape 1 — Mise en production

Proposée après un point d'étape (2026-09-17) sur ce qu'il reste avant que le site puisse tourner en dehors de `localhost`. Hébergement confirmé : VPS KVM **1 Go RAM** chez By-Hoster (F1 révisé — le doute initial sur 512 Mo levé après mesure réelle : `web` seul consomme déjà ~80 Mo au repos, sans `bot`/`jobs`/Postgres/reverse proxy). Statut : **proposition, pas encore exécutée** — à valider/ajuster avant de commencer, plusieurs items dépendent d'actions externes (By-Hoster, portail développeur Discord, Le_Club) que ni le code ni cette session ne peuvent faire à la place d'Arthus.

| # | Item | Bloque quoi | Qui |
|---|---|---|---|
| E1 | ~~Committer le travail en cours~~ **fait, 2026-09-17** (commit `32fd155`, 31 fichiers, A16→A33) — reste à pousser sur un remote quand il y en aura un, non fait ici | Tout le reste — rien à déployer tant que ce n'est pas sur une branche/un remote | Dev (Arthus) |
| E2 | Nom de domaine (F7, toujours "à définir/acquérir" au moment de Phase I) | E4 (TLS), E5 (redirect URI OAuth) — sans domaine, pas de `PUBLIC_BASE_URL` https valide, donc le process refuse de démarrer en prod | Client/Dev — tâche différée depuis Phase I, redevient bloquante ici |
| E3 | Provisionner le VPS (1 Go, By-Hoster, KVM Proxmox — F1), OS minimal, swap configuré par sécurité même à 1 Go | E4-E9 | Dev, chez By-Hoster |
| E4 | Reverse proxy (nginx ou Caddy) + certificat TLS (Let's Encrypt — Caddy peut l'automatiser) devant `web` | Le login Discord — sans TLS réel, les cookies `secure: true` (`session.js`, `oauth.js`) ne sont jamais envoyés par le navigateur, la connexion échoue silencieusement même si le process tourne | Dev |
| E5 | ~~Créer une application Discord~~ **existe déjà** (`DISCORD_CLIENT_ID`/`_SECRET`/`_BOT_TOKEN`/`_HUB_GUILD_ID` déjà en place en dev, confirmés dans `.env` — c'est elle qui a fait tourner tout l'OAuth et les invitations bot de cette session). Reste à trancher : réutiliser cette même app en ajoutant la redirect URI du domaine de prod à ses réglages OAuth2 (le Client ID/Secret/Bot Token ne changent pas), ou en créer une deuxième dédiée à la prod (identité bot séparée du dev — recommandé si des tests continuent de tourner en dev après la mise en ligne, pour ne pas mélanger les deux dans les logs/l'historique Discord du bot). Toute nouvelle invitation (prod comme dev) doit repartir de l'URL corrigée par A34 (`buildBotInviteUrl` : scopes `bot`+`applications.commands`, permission `Create Instant Invite` ajoutée) — une guilde invitée avant A34 n'a ni l'un ni l'autre tant qu'elle n'est pas réinvitée. | Redirect URI OAuth pour le domaine de prod ; identité du bot si séparation choisie ; guilde de prod invitée avec le scope/permissions à jour dès le départ | Dev, portail développeur Discord (juste un réglage, pas une création) |
| E6 | Postgres provisionné sur le VPS, migrations rejouées (`npm run migrate`) | Tout | Dev |
| E7 | Stratégie de backup a minima (`pg_dump` planifié) — rien n'existe aujourd'hui | Pas bloquant pour démarrer, mais aucun filet en cas de perte de données dès le premier jour | Dev |
| E8 | Secrets de prod générés (`SESSION_SECRET`, `TOKEN_ENC_KEY` — jamais réutiliser ceux de dev, ni l'un pour l'autre, cf. M8/`env.js`) | Sécurité des sessions et des jetons OAuth chiffrés au repos | Dev |
| E9 | Supervision des 3 process (`web`/`bot`/`jobs`) — `systemd` déjà visé par l'architecture (`2-Architecture.md`) mais rien de configuré ; sans ça, un crash ou un reboot du VPS ne relance rien tout seul | Fiabilité, pas le démarrage initial | Dev |
| E10 | ~~Premier admin~~ **tranché, 2026-09-17** : Arthus (dev) sera le `proprietaire` originel en prod, via le même bootstrap `INSERT` SQL manuel que celui utilisé en dev pour `arde_ass`/A32 — pas de mécanisme self-service construit, décision assumée plutôt qu'un oubli (cf. `4-Audit.md`). Une fois connecté et son compte lié à ce rôle, Arthus attribue ensuite le rôle admin à Le_Club depuis le panel (`/admin/users/:id` -> gestion des rôles, `rbac.assignRole`, déjà fonctionnel — aucun code à écrire, la capacité existe depuis A27) : Le_Club devient administrateur de sa propre plateforme sans jamais toucher à la base directement. | Accès au panel admin en prod | Dev — décidé ; exécution au moment du déploiement (premier `INSERT`), puis via le panel pour Le_Club |
| E11 | Vérification post-déploiement : login Discord réel, bot rejoint une guilde de test, publication/essai/transfert simulé de bout en bout, panel admin accessible | Confiance que le déploiement marche, pas juste qu'il démarre | Dev, en live |

Hors Étape 1, non bloquants pour un premier déploiement mais toujours ouverts : sign-off client sur A19 (contractuel, pas technique) — **fait, validé 2026-09-17**, voir A19 ; le gap `onGuildDelete` ne désassigne pas l'ownership directement (A26, filet existant via resynchronisation OAuth), Q1 (canal de notification hors-site, non tranché).

~~**Trouvé en testant A34 (2026-09-17)** : le bot n'était plus membre de la guilde configurée en `DISCORD_HUB_GUILD_ID` (1546194397987610694, "Xyro Market Hub")~~ **résolu, 2026-09-17** : bot réinvité sur "Xyro Market Hub" avec le lien corrigé par A34 (scopes `bot applications.commands`, permission `Create Instant Invite` incluse) — `GET /guilds/1546194397987610694` confirme `200` (`Xyro Market`, rôle avec permissions `361045690497`). Bot redémarré, `settings.hub_invite_url` régénéré (`ensureHubInvite` a tourné sans erreur). **Vérification en direct du contenu réel des messages A34**, jusque-là impossible faute d'accès à la guilde hub : les trois événements ont été republiés sur une vraie transaction réelle déjà `EXPIRED` (`4d426732-0fe1-4dc4-b310-ab5b822aeef8`, guilde `Cacahuètes et Kkhuètes`, comptes réels `arde_ass`/`dy_epic`) directement dans son fil hub réel (`1546848136784515133`) — les trois messages sont bien apparus, texte exact confirmé par lecture REST du fil : `⏰ La période d'essai se termine le 17/09/2026 03:57:20. ...`, `❌ Échange terminé — la période d'essai s'est achevée sans validation des deux parties, l'échange est caduc.`, `✅ Échange terminé — le transfert de propriété a été confirmé.`. Seul le message d'invitation (volet 1 d'A34) n'a pas pu être revérifié visuellement dans cette passe — son mécanisme (`thread.send`) est strictement le même que celui déjà prouvé par les trois messages ci-dessus, sur le même fil, avec le même client. E11 (vérification post-déploiement) reste néanmoins à refaire une fois en prod, sur un scénario neuf de bout en bout.
